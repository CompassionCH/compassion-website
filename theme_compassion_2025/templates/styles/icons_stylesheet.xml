<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="theme_compassion_icons_stylesheet_template" name="Icons Stylesheet Template">
        <t t-translation="off">
<!-- Base styles for any element using an icon class. -->
<!-- This ensures consistency and leverages the `currentColor` change you made. -->
[class^="icon-"],
[class*=" icon-"] {
    display: inline-block;
    <!-- Use 1em to make the icon size relative to the font-size of its parent -->
    width: 1.2em;
    height: 1.2em;
    <!-- `currentColor` allows the icon color to be set with the CSS `color` property -->
    background-color: currentColor;
    <!-- Helps align icons with text -->
    vertical-align: middle;
    <!-- Use `mask` to display the SVG shape, allowing the background-color to show through -->
    mask-size: contain;
    mask-repeat: no-repeat;
    mask-position: center;
}

<t t-foreach="icons" t-as="icon">
.<t t-esc="icon.class_name"/> {
    mask-image: url("/web/image/theme.compassion.icons/<t t-esc="icon.id"/>/svg_file");
}
</t>
        </t>
    </template>
</odoo>
