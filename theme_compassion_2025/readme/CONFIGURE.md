The footer renders its Policies link and social icons from fields on the
website record: `privacy_policy_url`, `social_vimeo` (added by this theme) and
the standard `social_facebook`, `social_instagram`, `social_youtube`,
`social_linkedin`. An icon is hidden while its field is empty. Set the values
per website (website form, or the standard social fields default from the
company).
