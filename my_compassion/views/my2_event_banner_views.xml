<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- List View -->
    <record id="view_my2_compassion_event_banner_tree" model="ir.ui.view">
        <field name="name">my_compassion.event_banner.tree</field>
        <field name="model">my_compassion.event_banner</field>
        <field name="arch" type="xml">
            <tree string="Event Banners">
                <field name="banner_title"/>
                <field name="is_active"/>
                <field name="start_date"/>
                <field name="end_date"/>
            </tree>
        </field>
    </record>

    <!-- Form -->
    <record id="view_my2_compassion_event_banner_form" model="ir.ui.view">
        <field name="name">my_compassion.event_banner.form</field>
        <field name="model">my_compassion.event_banner</field>
        <field name="arch" type="xml">
            <form string="Event Banner">
                <sheet>
                    <group string="Content">
                        <group>
                            <field name="banner_title"/>
                            <field name="banner_description"/>
                        </group>
                        <group>
                            <field name="pictogram" />
                            <field name="button_text" />
                        </group>

                    </group>
                    <group string="Appearance">
                        <group>
                            <field name="banner_background_color" />
                            <field name="banner_text_color" />
                            <field name="pictogram_color" />
                        </group>
                        <group>
                            <field name="button_background_color" />
                            <field name="button_text_color" />
                        </group>
                    </group>
                    <group string="Configuration">
                        <group>
                            <field name="button_action_url" />
                            <field name="target_pages" placeholder=" Separate multiple URLs with a semicolon (;) e.g. /my2/dashboard;/my2/gits"/>
                            <!--<field name="target_pages" widget="many2many_tags" placeholder="Select pages where the banner should appear..."/> -->
                            <field name="is_active"/>
                        </group>
                        <group>
                            <field name="start_date"/>
                            <field name="end_date"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Custom Filter -->
    <record id="view_my2_compassion_event_banner_search" model="ir.ui.view">
        <field name="name">my_compassion.event_banner.search</field>
        <field name="model">my_compassion.event_banner</field>
        <field name="arch" type="xml">
            <search string="Search Event Banners">
                <field name="banner_title" string="Banner Title"/>
                <separator/>
                <filter string="Currently Active Banners" name="currently_active"
                        domain="[('is_active', '=', True),
                                 ('start_date', '&lt;=', now),
                                 '|',
                                 ('end_date', '=', False),
                                 ('end_date', '&gt;=', now)]"/>
            </search>
        </field>
    </record>

    <!-- Window action -->
    <record id="action_my2_compassion_event_banner" model="ir.actions.act_window">
        <field name="name">Event Banners</field>
        <field name="res_model">my_compassion.event_banner</field>
        <field name="view_mode">tree,form</field>
        <field name="search_view_id" ref="view_my2_compassion_event_banner_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new event banner.
            </p>
        </field>
    </record>

    <!-- Menu Item -->
    <menuitem
        id="menu_my2_compassion_event_banner"
        name="Event Banners"
        parent="website.menu_website_configuration"
        action="action_my2_compassion_event_banner"
        sequence="100"/>

</odoo>
