<?xml version="1.0" encoding="utf-8" ?>
<!--
    Component: Giving Limits Table

    Description:
    A table displaying the giving limits.

    Props:                                                                                       Default:
    - limits (list[gift.threshold.settings]): The price limits for different donations types.    -required-
    - date (date): The date to use for price conversions.                                        [today]
-->
<odoo>
  <!-- GIVING LIMITS TABLE COLUMN -->
  <template
    id="my2_giving_limits_modal_table_column"
    name="Gift Package Help Table Column"
  >
    <div t-attf-class="table-column d-flex flex-column #{top_class}">
      <div
        class="table-cell tiny-text bold bg-light-green border-0 d-flex flex-column justify-content-center"
      >
        <t t-out="title" />
      </div>
      <t t-foreach="cells" t-as="cell">
        <div
          class="table-cell super-tiny-text d-flex flex-column justify-content-center"
        >
          <div>
            <t t-out="cell.replace('*', '')" />
            <span t-if="'*' in cell" class="body-small text-low-pink">*</span>
          </div>
        </div>
      </t>
    </div>
  </template>
  <!-- GIVING LIMITS TABLE COMPONENT -->
  <template id="GivingLimitsTableComponent" name="Giving Limits Table Component">
    <t t-set="company" t-value="request.website.company_id" />
    <t t-set="date" t-value="date or datetime.date.today()" />
    <div class="giving-limits-table d-flex flex-column">
      <!-- TABLE TITLE -->
      <div
        class="p-3 table-title col-12 bg-low-green tiny-text text-center text-pure-white"
      >
                Annual gift limits
            </div>
      <!-- COLUMNS -->
      <div class="d-flex">
        <!-- TYPE -->
        <t t-call="my_compassion.my2_giving_limits_modal_table_column">
          <t t-set="top_class" t-value="'flex-grow-1'" />
          <t t-set="title">Type</t>
          <t
            t-set="cells"
            t-value="[
                                        limit.gift_type_id.name
                                        + limit.get_gift_frequency_indicator()
                                        for limit in limits
                                    ]"
          />
        </t>
        <!-- FREQUENCY -->
        <t t-call="my_compassion.my2_giving_limits_modal_table_column">
          <t t-set="title">Frequency</t>
          <t
            t-set="cells"
            t-value="[
                        limit.get_gift_frequency_label()
                        for limit in limits
                    ]"
          />
        </t>
        <!-- AMOUNT -->
        <t t-call="my_compassion.my2_giving_limits_modal_table_column">
          <t t-set="title">Amount</t>
          <t
            t-set="cells"
            t-value="[
                        limit.get_amount_range_label(company, date)
                        for limit in limits
                    ]"
          />
        </t>
      </div>
      <!-- NOTES -->
      <div class="px-3 pt-1 super-tiny-text text-low-pink">
                *Available in the child's final year of the program.
            </div>
    </div>
  </template>
</odoo>
