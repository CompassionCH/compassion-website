<!--
    Component: Donation Item

    Description:
    An item representing a donation/sponsorship the user will pay, has paid or is paying for.

    Button:                                                                                  Default:
    - icon (string): The button icon.                                                        [none]
    - pictogram (string): The button pictogram.                                              [none]
    - label (string): The button label.                                                      [none]
    - color (string): The button color.                                                      [dark-blue]
    - action (string): Either 'link' or 'event'.                                             [none]
    - href (string) [only if action='link']: The url of the link.                            ['']
    - custom_event (string) [only if action='event']: The custom event name.                 [{}]
    - detail (dict) [only if action='event']: A custom object to set as the event detail.    [{}]

    Props:                                                                                   Default:
    - name (string): Item name.                                                              -required-
    - image (string): Item image src.                                                        -required-
    - price (float): Item price.                                                             -required-
    - currency_name (str): Currency name                                                     -required-
    - primary_button (dict): Primary button with custom action (see Button).                 [none]
    - recipient (string): Item recipient.                                                    [none]
    - subtext (string): Text under name and recipient.                                       [none]
    - image_fallback_pictogram : Pictogram to display instead of the image when it's None    [none]
    - title_color (string): Color of the title.                                              [dark-blue]
    - accent_color (string): Color of the left border accent.                                [dark-blue]
    - price_color (string): Color of the price.                                              [dark-blue]
    - recipient_color (string): Color of the recipient.                                      [dark-blue]
    - bg_color (string): Background color.                                                   [high-blue]
    - monthly (bool): Whether the price is paid monthly (with abbreviation).                 [False]
    - monthly_noabbr (bool): Whether the price is paid monthly (no abbreviation).            [False]
    - secondary_buttons (list[dict]): Secondary buttons with custom actions (see Button).    [none]
    - top_class (string): Classes to add to the top element of the item.                     [none]
    - price_label (string): Text shown instead of the formatted price/currency,              [none]
      for items with nothing to charge (e.g. Write&amp;Pray's free-godparent branch).
    - price_id (string): id attribute on the price element, so a caller's own JS             [none]
      can live-update it (e.g. as a Write&amp;Pray amount is picked).
-->
<odoo>
  <!-- DONATION ITEM BUTTON -->
  <template id="DonationItemButton" name="Donation Item Button">
    <t t-set="color" t-value="button.get('color') or 'dark-blue'" />
    <t
      t-if="button.get('action') == 'link'"
      t-set="onclick"
      t-valuef="location.href='#{button.get('href')}'"
    />
    <t
      t-elif="button.get('action') == 'event'"
      t-set="onclick"
      t-valuef="this.dispatchEvent(new CustomEvent('#{button.get('custom_event')}', {bubbles: true, detail: #{button.get('detail')}}))"
    />
    <div t-attf-class="action-button #{button_top_class}" t-att-onclick="onclick">
      <i
        t-if="button.get('icon')"
        t-attf-class="tiny-text icon-#{button.get('icon')} text-#{color}"
      />
      <i
        t-if="button.get('pictogram')"
        t-attf-class="tiny-text #{button.get('pictogram')} text-#{color}"
      />
      <span t-if="button.get('label')" t-attf-class="tiny-text bold text-#{color}">
        <t t-out="button.get('label')" />
      </span>
    </div>
  </template>
  <!-- DONATION ITEM COMPONENT -->
  <template id="DonationItemComponent" name="Donation Item Component">
    <!-- Default property values -->
    <t t-set="title_color" t-value="title_color or 'dark-blue'" />
    <t t-set="accent_color" t-value="accent_color or 'dark-blue'" />
    <t t-set="price_color" t-value="price_color or 'dark-blue'" />
    <t t-set="recipient_color" t-value="recipient_color or 'dark-blue'" />
    <t t-set="subtext_color" t-value="subtext_color or 'dark-blue'" />
    <t t-set="bg_color" t-value="bg_color or 'high-blue'" />
    <t t-set="monthly" t-value="monthly == True" />
    <t t-set="monthly_noabbr" t-value="monthly_noabbr == True" />
    <t t-set="secondary_buttons" t-value="secondary_buttons or []" />
    <!-- Estimate component minimum width -->
    <t t-set="min_width" t-valuef="#{300 + 24 * len(secondary_buttons)}px" />
    <!--Small image on the card  -->
    <t t-set="small_thumbnail_content">
      <div
        t-attf-class="thumbnail responsive-sm me-3 flex-shrink-0 rounded-lg #{not image and image_fallback_pictogram or ''}"
        t-attf-style="--image_url: url('#{image}');"
      />
    </t>
    <div
      t-attf-class="donation-item-container p-2 d-flex rounded rounded-lg bg-#{bg_color} border-#{accent_color} #{top_class}"
      t-attf-style="min-width: #{min_width}"
    >
      <div
        t-attf-class="thumbnail responsive-lg me-3 flex-shrink-0 rounded rounded-lg #{not image and image_fallback_pictogram or ''}"
        t-attf-style="--image_url: url('#{image}');"
      />
      <t t-if="not primary_button" t-out="small_thumbnail_content" />
      <div class="w-100 d-flex flex-column justify-content-between">
        <div class="d-flex align-items-start">
          <t t-if="primary_button" t-out="small_thumbnail_content" />
          <div class="flex-grow-1 d-flex flex-column">
            <div t-attf-class="text-#{title_color} body-large bold">
              <t t-out="name" />
            </div>
            <div
              t-if="recipient"
              t-attf-class="text-#{recipient_color} body-small bold"
            >
                            For
                            <t t-out="recipient" />
                        </div>
            <div t-if="subtext" t-attf-class="text-#{subtext_color} body-small bold">
              <t t-out="subtext" />
            </div>
          </div>
          <t t-foreach="secondary_buttons" t-as="button">
            <t t-call="my_compassion.DonationItemButton">
              <t t-set="button_top_class" t-value="'ms-2'" />
            </t>
          </t>
        </div>
        <div
          t-attf-class="mt-2 d-flex justify-content-#{'between' if primary_button else 'end'}"
        >
          <t t-if="primary_button" t-call="my_compassion.DonationItemButton">
            <t t-set="button" t-value="primary_button" />
          </t>
          <div
            t-attf-class="text-#{price_color} "
            t-att-id="price_id"
            t-att-data-currency-name="currency_name"
          >
            <t t-if="price_label" t-out="price_label" />
            <t t-else="">
              <t t-out="currency_name" />
              <t
                t-if="price % 1 == 0"
                t-set="price"
                t-value="'{0}.-'.format(int(price))"
              />
              <t t-else="" t-set="price" t-value="'{0:.2f}'.format(price)" />
              <t t-if="monthly" t-set="monthly">/mo.</t>
              <t t-elif="monthly_noabbr">
                <t t-set="monthly">&#160;/month</t>
              </t>
              <t t-else="" t-set="monthly" />
              <t t-out="price + monthly" />
            </t>
          </div>
        </div>
      </div>
    </div>
  </template>
  <template id="SponsorshipDonationItem" name="Sponsorship Donation Item">
    <t t-set="label_see_profile">See profile</t>
    <t t-set="label_see_more">See more</t>
    <t t-if="sponsorship.type == 'CSP'">
      <t
        t-set="product_template"
        t-value="sponsorship.contract_line_ids[:1].product_id.product_tmpl_id"
      />
      <t t-if="product_template">
        <t
          t-set="csp_my_compassion_name"
          t-value="product_template.my_compassion_name"
        />
        <t t-set="csp_thanks_name" t-value="product_template.thanks_name" />
        <t t-set="csp_default_name">Compassion survival program</t>
        <t
          t-set="displayed_title"
          t-value="csp_my_compassion_name or csp_thanks_name or csp_default_name"
        />
        <t
          t-set="image_fallback_pictogram"
          t-value="product_template.my_compassion_pictogram.class_name or 'pictogram-gift-donation-general'"
        />
        <t t-if="product_template.is_published">
          <t
            t-set="displayed_image"
            t-value="website.image_url(product_template, 'my_compassion_image')"
          />
          <t
            t-set="primary_button_data"
            t-value="{
                'label': label_see_more,
                'pictogram': product_template.my_compassion_pictogram.class_name or 'pictogram-gift-donation-general',
                'action': 'link',
                'href': '/my2/gifts/' + str(product_template.id),
                'color': 'low-blue'
            }"
          />
        </t>
        <t t-set="secondary_buttons_data" t-value="[]" />
      </t>
    </t>
    <t t-else="">
      <t t-set="image_fallback_pictogram" t-value="'pictogram-child-sponsorship'" />
      <t t-set="displayed_title" t-value="sponsorship.child_id.preferred_name" />
      <t
        t-set="displayed_image"
        t-valuef="/web/image/compassion.child/#{sponsorship.child_id.id}/portrait"
      />
      <t
        t-set="primary_button_data"
        t-value="{
            'label': label_see_profile,
            'pictogram': 'pictogram-child-sponsorship',
            'action': 'link',
            'href': '/my2/children/' + str(sponsorship.child_id.id),
            'color': 'low-blue'
        }"
      />
      <t t-set="secondary_buttons_data" t-value="[]" />
    </t>
    <t t-call="my_compassion.DonationItemComponent">
      <t t-set="image_fallback_pictogram" t-value="image_fallback_pictogram" />
      <t t-set="name" t-value="displayed_title" />
      <t t-set="image" t-value="displayed_image" />
      <t t-set="subtext" t-value="sponsorship.payment_mode_id.display_name" />
      <t t-set="price" t-value="sponsorship.total_amount" />
      <t t-set="primary_button" t-value="primary_button_data" />
      <t t-set="secondary_buttons" t-value="secondary_buttons_data" />
      <t t-set="monthly_noabbr" t-value="True" />
      <t t-set="bg_color" t-value="'light-blue'" />
      <t t-set="accent_color" t-value="'low-blue'" />
      <t t-set="title_color" t-value="'low-blue'" />
      <t t-set="price_color" t-value="'low-blue'" />
    </t>
  </template>
</odoo>
