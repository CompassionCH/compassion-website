<?xml version="1.0" encoding="utf-8" ?>
<!--
    Description:

     Injected data:
    - modal_type: Type of modal to display (Update, Add, Change).
    - description: Description text to display in the modal.
    TODO : Add more injected data as needed for payment method options.
    (for instance, current payment method details, available payment methods list, etc.)
-->
<odoo>
    <template id="my2_payment_method_selector" name="Payment Method Selector Component">
        <div class="modal fade" id="payment_method_selector_modal" tabindex="-1" role="dialog" aria-hidden="true">
            <div class="modal-dialog modal-dialog-centered" role="document">
                <div class="modal-content border-0">
                    <div class="p-0 m-0">

                        <h3 class="text-center text-core-blue pb-2 pt-3">
                            <t t-esc="modal_type + ' Payment Method'"/>
                        </h3>

                        <div class="pl-4 pr-4 pt-3">
                            <form id="payment_method_form">
                                <input type="hidden" name="contract_group_id" id="selected_contract_group_id"/>

                                <t t-esc="description"/>

                            </form>



                            <div class="modal-footer border-top-0">
                                <a href="#" data-dismiss="modal">
                                    <t t-call="theme_compassion_2025.ThemedButtonComponent">
                                        <t t-set="variant" t-value="'default'"/>
                                        <t t-set="variation" t-value="'hollow'"/>
                                        <t t-set="label" t-value="'Cancel'"/>
                                        <t t-set="color" t-value="'core-blue'"/>
                                        <t t-set="text_color" t-value="'core-blue'"/>
                                    </t>
                                </a>

                                <t t-call="theme_compassion_2025.ThemedButtonComponent">
                                    <t t-set="variant" t-value="'default'"/>
                                    <t t-set="variation" t-value="'filled'"/>
                                    <t t-set="label" t-value="'Save Changes'"/>
                                    <t t-set="color" t-value="'core-blue'"/>
                                    <t t-set="text_color" t-value="'white'"/>
                                    <t t-set="id" t-value="'btn_save_payment_method'"/>
                                </t>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </template>

    <template id="my2_sponsorships_group_card" name="Sponsorships Group Card Component">
        <div class="sponsorship-group mb-4">
            <div class="d-flex justify-content-between align-items-center p-3 bg-light rounded cursor-pointer shadow-sm border"
                 data-toggle="collapse"
                 t-att-data-target="'#group_collapse_%s' % group.id"
                 aria-expanded="true">

                <div class="d-flex align-items-center">
                    <h4 class="m-0 text-dark-blue font-weight-bold">
                        <t t-esc="group.payment_mode_id.display_name or 'Unknown Method'"/>
                    </h4>
                    <span class="ml-3 badge badge-pill badge-info">
                        <t t-esc="group.total_amount" />
                        <t t-if="group.contract_ids" t-esc="group.contract_ids[0].pricelist_id.currency_id.symbol"/>
                    </span>
                </div>
                <div class="d-flex align-items-center">
                    <div>
                        <i class="fa fa-edit text-core-black mr-3 cursor-pointer"
                           id="edit_payment_method_button"/>
                    </div>
                    <div class="text-core-black">
                        <i class="fa fa-chevron-down"/>
                    </div>
                </div>
            </div>

            <div t-att-id="'group_collapse_%s' % group.id" class="collapse show mt-3 pl-2 pr-2">
                <t t-foreach="group.contract_ids" t-as="sponsorship">
                    <t t-if="sponsorship">
                        <t t-if="sponsorship.state not in ['terminated', 'cancelled']">
                            <div class="mb-3">
                                <t t-set="primary_button_data" t-value="{
                                    'label': 'See profile',
                                    'pictogram': 'child-sponsorship',
                                    'action': 'link',
                                    'href': '/my2/children/' + str(sponsorship.child_id.id),
                                    'color': 'low-blue'
                                }"/>
                                <t t-set="secondary_buttons_data" t-value="[
                                    {
                                        'icon': 'download01',
                                        'color': 'mid-yellow',
                                        'action': 'link',
                                        'href': '/my/download/gift_bvr?child_id=' + str(sponsorship.child_id.id),
                                    }
                                ]"/>

                                <t t-call="my_compassion.DonationItemComponent">
                                    <t t-set="name" t-value="sponsorship.child_id.preferred_name" />
                                    <t t-set="image" t-valuef="/web/image/compassion.child/#{sponsorship.child_id.id}/portrait"/>
                                    <t t-set="subtext" t-value="sponsorship.payment_mode_id.display_name" />
                                    <t t-set="price" t-value="sponsorship.total_amount" />
                                    <t t-set="primary_button" t-value="primary_button_data" />
                                    <t t-set="monthly_noabbr" t-value="True" />
                                    <t t-set="secondary_buttons" t-value="secondary_buttons_data" />
                                    <t t-set="bg_color" t-value="'light-blue'" />
                                    <t t-set="accent_color" t-value="'low-blue'" />
                                    <t t-set="title_color" t-value="'low-blue'" />
                                    <t t-set="price_color" t-value="'low-blue'" />
                                </t>
                            </div>
                        </t>
                    </t>
                </t>
            </div>
        </div>
    </template>
</odoo>
