<?xml version="1.0" encoding="utf-8" ?>
<!--
Page: My Compassion 2 New Sponsorship Wizard Page

    Description:
    This page guides the user through the sponsorship procedure. Which steps
    run is decided by new.sponsorship.wizard.STEPS_CONFIGS / _get_step_xmlids;
    every template below is one of them. Public visitors run the fast
    checkout - one page holding the email, the consent and one button per
    payment mode - and give their name after paying; the user-details and
    communication steps are kept for the flows and country extensions that
    still collect everything up front.

    Used in route:
    - /my2/new-sponsorship/<child-id>
    - /my2/new-sponsorship/thank-you
    - /my2/new-sponsorship/step

    Injected data:
    - wizard ('new.sponsorship.wizard' record): The associated new sponsorship wizard record.
    - titles (array of 'res.partner.title' records): The available titles to choose from.
    - countries (array of 'res.country' records): The list of countries to choose from.
    - payment_methods (array of 'account.payment.mode' records): The list of payment methods to choose from.
    - payment_mode_buttons (array of 'account.payment.mode' records): The
      modes the current step submits itself with, one button each (see
      new.sponsorship.wizard._get_payment_mode_buttons). Empty for the steps
      that keep the generic Continue/Finish button.
    - step_offers_payment_modes (boolean): Whether those buttons are the
      current step's submit (see
      new.sponsorship.wizard._step_offers_payment_modes). False for the steps
      that keep the generic button; True with an empty payment_mode_buttons
      means the company has published no mode, and the page must then offer
      no submit at all.
    - spoken_languages (array of 'res.lang.compassion' records): The list of spoken languages to choose from.
    - lead_sources (array of 'recurring.contract.origin' records): The list of lead sources to choose from.
    - details_token (string): Thank-you page only. Set while the signup still
      waits for its sponsor's name, and what makes the page show the
      "Who shall we thank?" form (my2_new_sponsorship_details.xml).

    Note:
    The element IDs #payment_method and #finish_button are used by the
    Compassion Switzerland eBill extension (my_compassion_switzerland addon).
    If renamed or modified, make sure to update the corresponding logic there as well.
    They live on the payment-methods step, which the logged-in flow still
    runs; the public fast-checkout page has neither, so that extension is
    inert on it until it is ported onto the mode buttons.
-->
<odoo>
  <!-- NEW SPONSORSHIP STEP PROGRESS BAR -->
  <template id="NewSponsorshipStepProgressBar" name="New Sponsorship Step Progress Bar">
    <!-- Default property values -->
    <t t-set="n_steps" t-value="n_steps or 3" />
    <t t-set="current_step" t-value="current_step or 0" />
    <t t-set="marker_color" t-value="marker_color or 'mid-yellow'" />
    <t t-set="progress_color" t-value="progress_color or 'low-yellow'" />
    <t t-set="track_color" t-value="track_color or 'low-eggshell'" />
    <!-- Step progress bar -->
    <div class="step-progress-container">
      <!-- Track -->
      <div t-attf-class="step-progress-track d-flex">
        <t t-foreach="range(n_steps)" t-as="i">
          <div
            t-if="i &lt; current_step"
            t-attf-class="flex-grow-1 bg-#{progress_color}"
          />
          <div t-elif="i > current_step" t-attf-class="flex-grow-1 bg-#{track_color}" />
        </t>
      </div>
      <!-- Steps -->
      <t t-if="n_steps == 1" t-set="justify_content" t-value="'center'" />
      <t t-else="" t-set="justify_content" t-value="'between'" />
      <div t-attf-class="d-flex justify-content-#{justify_content}">
        <t t-foreach="range(n_steps)" t-as="i">
          <div class="step-marker-container">
            <div
              t-if="i &lt; current_step"
              t-attf-class="step-marker-active bg-#{marker_color} border-#{marker_color}"
            />
            <div
              t-elif="i == current_step"
              t-attf-class="step-marker-active bg-#{progress_color} border-#{marker_color}"
            />
            <div t-else="" t-attf-class="step-marker bg-#{track_color}" />
          </div>
        </t>
      </div>
    </div>
  </template>
  <!-- NEW SPONSORSHIP WIZARD FAST CHECKOUT STEP -->
  <template
    id="my2_new_sponsorship_wizard_fast_checkout"
    name="New Sponsorship Wizard Fast Checkout Step"
  >
    <!-- Set only by the Write&Pray page's own age check
         (my2_new_sponsorship_wizard.js::_checkWAPAge), which redirects
         here instead of offering a modal - there is nothing left to do on
         that page once the sponsor is ineligible. -->
    <div
      t-if="request.params.get('wap_ineligible')"
      class="mb-3 p-3 rounded-lg bg-high-eggshell tiny-text"
    >
            Write&amp;Pray is for people under 25, so here's the regular sponsorship instead.
        </div>
    <div class="mb-3 p-3 rounded-lg bg-light-blue tiny-text">
            Already have an account?
            <a
      t-att-href="login_url_redirect"
      class="tiny-text bold text-core-blue"
    >Log in</a>
        </div>
    <!-- Child summary: what the sponsor is committing to, at a glance,
         before the email/payment fields below. Reuses the same generic
         row used for the donation history (my2_donation_item.xml) rather
         than the full my2_children_card.xml profile card, which is sized
         and laid out for a standalone profile tile, not an inline summary. -->
    <div class="mb-3">
      <t t-call="my_compassion.DonationItemComponent">
        <t t-set="name"><t
          t-if="wizard.sponsorship_type == 'write_and_pray'"
        >Write&amp;Pray</t><t t-else="">Sponsorship</t>&#160;·&#160;<t
          t-out="wizard.child_id.preferred_name"
        />, <t t-out="wizard.child_id.age" /></t>
        <t t-set="subtext"><t
          t-out="wizard.child_id.field_office_id.country_id.name"
        />&#160;·&#160;monthly</t>
        <t
          t-set="image"
          t-valuef="/web/image/compassion.child/#{wizard.child_id.id}/portrait"
        />
        <t t-set="image_fallback_pictogram" t-value="'pictogram-child-sponsorship'" />
        <t t-set="price" t-value="float(sponsorship_amount or 0)" />
        <t t-set="currency_name" t-value="currency_name" />
        <t t-set="bg_color" t-value="'light-blue'" />
        <t t-set="accent_color" t-value="'low-blue'" />
        <t t-set="title_color" t-value="'low-blue'" />
        <t t-set="price_color" t-value="'low-blue'" />
      </t>
    </div>
    <!-- Sponsorship plus -->
    <t
      t-if="wizard.sponsorship_type != 'write_and_pray'"
      t-call="my_compassion.my2_new_sponsorship_wizard_sponsorship_plus"
    />
    <!-- Email -->
    <t t-call="theme_compassion_2025.FormFieldComponent">
      <t t-set="top_class" t-value="'my-3'" />
      <t t-set="label">E-mail</t>
      <t t-set="input_id" t-value="'email'" />
      <input
        id="email"
        type="email"
        name="email"
        class="form-control"
        placeholder="you@example.com"
        t-att-value="wizard.email"
        data-validate-type="email"
        required=""
      />
      <div
        class="mt-2 very-tiny-text text-mid-grey"
      >For your receipt and news about&#160;<t
        t-out="wizard.child_id.preferred_name"
      />. Nothing else is needed right now.</div>
    </t>
    <!-- Privacy consent -->
    <input type="hidden" name="privacy_consent" value="" />
    <t t-call="theme_compassion_2025.FormFieldComponent">
      <t t-set="top_class" t-value="'my-3'" />
      <div class="d-flex align-items-center">
        <input
          type="checkbox"
          id="privacy_consent"
          name="privacy_consent"
          class="flex-shrink-0"
          t-att-checked="wizard.privacy_consent"
          required=""
        />
        <label class="ms-2 mb-0 tiny-text text-mid-grey" for="privacy_consent">
                    I accept the privacy policy as described in the
                    <a
          href="/legal"
          class="tiny-text bold text-core-blue"
        >privacy notice</a>.
                </label>
      </div>
    </t>
    <!-- The payment-mode buttons that end this page are rendered by
         my2_new_sponsorship_wizard_form_content, next to the generic
         Continue/Finish button they stand in for. -->
  </template>
  <!-- NEW SPONSORSHIP WIZARD SPONSORSHIP PLUS -->
  <!-- Shared by the fast-checkout page and the logged-in payment step: both
       offer the same opt-in, and it has to read the same on both. -->
  <template
    id="my2_new_sponsorship_wizard_sponsorship_plus"
    name="New Sponsorship Wizard Sponsorship Plus"
  >
    <div class="my-3">
      <input type="hidden" name="sponsorship_plus" value="" />
      <label
        for="sponsorship_plus"
        class="toggle-card d-flex align-items-center p-3 rounded rounded-lg bg-light-blue border border-mid-grey"
      >
        <input
          type="checkbox"
          id="sponsorship_plus"
          name="sponsorship_plus"
          class="d-none"
          t-att-checked="wizard.sponsorship_plus"
        />
        <span
          class="toggle-card-check me-3 flex-shrink-0 d-flex align-items-center justify-content-center rounded-circle bg-core-blue text-pure-white tiny-text bold"
        >✓</span>
        <span class="flex-grow-1">
          <span
            class="d-block text-dark-blue very-tiny-text bold"
          >Sponsorship plus</span>
          <span class="d-block text-mid-grey very-tiny-text"><t
            t-out="currency_name"
          />&#160;<t t-out="sponsorship_amount" />.- + <t
            t-out="currency_name"
          />&#160;<t t-out="sponsorship_plus_extra" />.- solidarity fund</span>
        </span>
        <span
          class="toggle-card-price ms-3 text-core-blue very-tiny-text bold flex-shrink-0"
        >+&#160;<t t-out="currency_name" />&#160;<t
          t-out="sponsorship_plus_extra"
        />.-</span>
      </label>
      <a
        href="#"
        class="d-inline-block mt-2 very-tiny-text bold text-core-blue"
        data-bs-toggle="modal"
        data-bs-target="#sponsorship-plus-info-modal"
      >What does the extra go to?</a>
    </div>
    <!-- Sponsorship plus info modal -->
    <div
      class="modal fade"
      id="sponsorship-plus-info-modal"
      tabindex="-1"
      role="dialog"
      aria-labelledby="sponsorship-plus-info-modal-title"
      aria-hidden="true"
    >
      <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content border-0">
          <div class="modal-body">
            <button
              type="button"
              class="btn-close mt-3 me-3 icon-x-circle body-large text-mid-grey"
              data-bs-dismiss="modal"
              aria-label="Close"
            />
            <div class="px-4 pt-2 pb-4">
              <div
                id="sponsorship-plus-info-modal-title"
                class="mb-2 text-dark-blue body-small bold"
              >What does the extra go to?</div>
              <div class="body-small">
                            Sponsorship+ includes the basic sponsorship of
                            <t t-out="currency_name" />
                            <t
                t-out="sponsorship_amount"
              />.-, plus an additional gift of
                            <t t-out="currency_name" />
                            <t t-out="sponsorship_plus_extra" />.- per month.
                            <br />
                            <br />
                            The extra amount helps to fund urgent needs not covered by the
                            sponsorship (e.g. natural disasters,
                            medical interventions, malaria prevention, etc.).
                            <br />
                            <br />
                            This fund is available for all children supported through Compassion.
                        </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </template>
  <!-- NEW SPONSORSHIP WIZARD USER DETAILS STEP -->
  <template
    id="my2_new_sponsorship_wizard_user_details"
    name="New Sponsorship Wizard User Details Step"
  >
    <div class="mb-3 p-3 rounded-lg bg-light-blue tiny-text">
            Already have an account?
            <a
      t-att-href="login_url_redirect"
      class="tiny-text bold text-core-blue"
    >Log in</a>
        </div>
    <!-- Salutation -->
    <t t-call="theme_compassion_2025.FormFieldComponent">
      <t t-set="top_class" t-value="'my-3'" />
      <t t-set="label">Salutation</t>
      <t t-set="input_id" t-value="'title'" />
      <t t-call="theme_compassion_2025.SelectComponent">
        <t t-set="name" t-value="'title'" />
        <t t-set="id" t-value="'title'" />
        <t t-set="required" t-value="True" />
        <option value="">Please select</option>
        <t t-foreach="titles" t-as="title">
          <option t-att-selected="title.id == wizard.title.id" t-att-value="title.id">
            <t t-out="title.name" />
          </option>
        </t>
      </t>
    </t>
    <!-- Surname -->
    <t t-call="theme_compassion_2025.FormFieldComponent">
      <t t-set="top_class" t-value="'my-3'" />
      <t t-set="label">Surname</t>
      <t t-set="input_id" t-value="'lastname'" />
      <input
        id="lastname"
        type="text"
        name="lastname"
        class="form-control"
        t-att-value="wizard.lastname"
        required=""
      />
    </t>
    <!-- Firstname -->
    <t t-call="theme_compassion_2025.FormFieldComponent">
      <t t-set="top_class" t-value="'my-3'" />
      <t t-set="label">Firstname</t>
      <t t-set="input_id" t-value="'firstname'" />
      <input
        id="firstname"
        type="text"
        name="firstname"
        class="form-control"
        t-att-value="wizard.firstname"
        required=""
      />
    </t>
    <!-- Date of birth -->
    <t t-call="theme_compassion_2025.FormFieldComponent">
      <t t-set="top_class" t-value="'my-3'" />
      <t t-set="label">Date of birth</t>
      <t t-set="input_id" t-value="'birthdate'" />
      <input
        id="birthdate"
        type="date"
        name="birthdate"
        class="form-control"
        t-att-value="wizard.birthdate"
        required=""
      />
    </t>
    <!-- Email -->
    <t t-call="theme_compassion_2025.FormFieldComponent">
      <t t-set="top_class" t-value="'my-3'" />
      <t t-set="label">E-mail</t>
      <t t-set="input_id" t-value="'email'" />
      <input
        type="email"
        name="email"
        class="form-control"
        t-att-value="wizard.email"
        data-validate-type="email"
        required=""
      />
    </t>
    <!-- Phone -->
    <t t-call="theme_compassion_2025.FormFieldComponent">
      <t t-set="top_class" t-value="'my-3'" />
      <t t-set="label">Phone number</t>
      <t t-set="input_id" t-value="'phone'" />
      <input
        id="phone"
        type="tel"
        name="phone"
        class="form-control"
        t-att-value="wizard.phone"
        data-validate-type="phone"
        required=""
      />
    </t>
    <!-- Street -->
    <div class="d-flex">
      <div class="w-100 flex-grow-1">
        <t t-call="theme_compassion_2025.FormFieldComponent">
          <t t-set="top_class" t-value="'my-3'" />
          <t t-set="label">Street</t>
          <t t-set="input_id" t-value="'street'" />
          <input
            id="street"
            type="text"
            name="street"
            class="form-control"
            t-att-value="wizard.street"
            required=""
          />
        </t>
      </div>
      <div class="flex-shrink-1 ms-4">
        <t t-call="theme_compassion_2025.FormFieldComponent">
          <t t-set="top_class" t-value="'my-3'" />
          <t t-set="label">N°</t>
          <t t-set="input_id" t-value="'street_number'" />
          <input
            id="street_number"
            type="text"
            name="street_number"
            class="form-control"
            t-att-value="wizard.street_number"
            required=""
          />
        </t>
      </div>
    </div>
    <!-- Locality -->
    <div class="d-flex">
      <div class="flex-shrink-1 me-4">
        <t t-call="theme_compassion_2025.FormFieldComponent">
          <t t-set="top_class" t-value="'my-3'" />
          <t t-set="label">Code</t>
          <t t-set="input_id" t-value="'zip'" />
          <input
            id="zip"
            type="text"
            name="zip"
            class="form-control"
            t-att-value="wizard.zip"
            required=""
          />
        </t>
      </div>
      <div class="w-100 flex-grow-1">
        <t t-call="theme_compassion_2025.FormFieldComponent">
          <t t-set="top_class" t-value="'my-3'" />
          <t t-set="label">Locality</t>
          <t t-set="input_id" t-value="'city'" />
          <input
            id="city"
            type="text"
            name="city"
            class="form-control"
            t-att-value="wizard.city"
            required=""
          />
        </t>
      </div>
    </div>
    <!-- Country -->
    <t t-call="theme_compassion_2025.FormFieldComponent">
      <t t-set="top_class" t-value="'my-3'" />
      <t t-set="label">Country</t>
      <t t-set="input_id" t-value="'country'" />
      <t t-call="theme_compassion_2025.SelectComponent">
        <t t-set="name" t-value="'country'" />
        <t t-set="id" t-value="'country'" />
        <t t-set="required" t-value="True" />
        <option value="">Please select</option>
        <t t-foreach="countries" t-as="country">
          <option
            t-att-selected="country.id == wizard.country.id"
            t-att-value="country.id"
          >
            <t t-out="country.name" />
          </option>
        </t>
      </t>
    </t>
  </template>
  <!-- NEW SPONSORSHIP WIZARD PAYMENT METHODS STEP -->
  <template
    id="my2_new_sponsorship_wizard_payment_method"
    name="New Sponsorship Wizard Payment Methods Step"
  >
    <!-- Payment method -->
    <t t-call="theme_compassion_2025.FormFieldComponent">
      <t t-set="top_class" t-value="'my-3'" />
      <t t-set="label">Payment method</t>
      <t t-set="input_id" t-value="'payment_method'" />
      <t t-call="theme_compassion_2025.SelectComponent">
        <t t-set="name" t-value="'payment_method'" />
        <!-- ID used by eBill extension in my_compassion_switzerland -->
        <t t-set="id" t-value="'payment_method'" />
        <t t-set="required" t-value="True" />
        <option value="">Please select</option>
        <t t-foreach="payment_methods" t-as="payment_method">
          <option
            t-att-selected="payment_method.id == wizard.payment_method.id"
            t-att-value="payment_method.id"
            t-att-data-payment-code="payment_method.payment_method_id.code"
          >
            <t t-out="payment_method.name" />
          </option>
        </t>
      </t>
    </t>
    <!-- Sponsorship plus -->
    <t t-call="my_compassion.my2_new_sponsorship_wizard_sponsorship_plus" />
  </template>
  <!-- NEW SPONSORSHIP WIZARD WRITE & PRAY FAST CHECKOUT STEP -->
  <!-- The public write_and_pray flow's only step (see STEPS_CONFIGS /
       WAP_FAST_CHECKOUT_STEP) - the same one-page shape as the standard
       fast checkout, but asking its own questions: date of birth (the
       eligibility gate), the free-godparent-vs-contributing choice, and
       only once contributing is picked, a suggested amount. Ends with the
       generic Continue button (see _step_offers_payment_modes) rather than
       payment-mode buttons: a Write&Pray sponsorship is never collected,
       so there is no mode to choose regardless of the amount it names. The
       logged-in flow still runs the older, plainer
       my2_new_sponsorship_wizard_wap_options step below unchanged. -->
  <template
    id="my2_new_sponsorship_wizard_wap_fast_checkout"
    name="New Sponsorship Wizard Write &amp; Pray Fast Checkout Step"
  >
    <div class="mb-3 p-3 rounded-lg bg-light-blue tiny-text">
            Already have an account?
            <a
      t-att-href="login_url_redirect"
      class="tiny-text bold text-core-blue"
    >Log in</a>
        </div>
    <!-- Child summary: what the sponsor is committing to, at a glance.
         Same DonationItemComponent the standard flow uses (see
         my2_new_sponsorship_wizard_fast_checkout above), via price_label to
         show "No payment" for the free-godparent branch instead of a
         formatted amount, and price_id so the JS can still live-update it as
         an amount is picked (see _updateWAPPrice). -->
    <div class="mb-3">
      <t t-call="my_compassion.DonationItemComponent">
        <t t-set="name">Write&amp;Pray&#160;·&#160;<t
          t-out="wizard.child_id.preferred_name"
        />, <t t-out="wizard.child_id.age" /></t>
        <t t-set="subtext"><t
          t-out="wizard.child_id.field_office_id.country_id.name"
        />&#160;·&#160;you write and pray</t>
        <t
          t-set="image"
          t-valuef="/web/image/compassion.child/#{wizard.child_id.id}/portrait"
        />
        <t t-set="image_fallback_pictogram" t-value="'pictogram-child-sponsorship'" />
        <t t-set="price" t-value="float(wizard.wap_contribution_amount or 0)" />
        <t
          t-set="price_label"
          t-value="not wizard.wap_contribution_amount and 'No payment'"
        />
        <t t-set="price_id" t-value="'wap-child-price'" />
        <t t-set="currency_name" t-value="currency_name" />
        <t t-set="bg_color" t-value="'light-blue'" />
        <t t-set="accent_color" t-value="'low-blue'" />
        <t t-set="title_color" t-value="'low-blue'" />
        <t t-set="price_color" t-value="'low-blue'" />
      </t>
    </div>
    <!-- Date of birth: always asked here, unlike the older wap_options step
         (which skips it once a previous step already set it - there is no
         previous step any more). -->
    <t t-call="theme_compassion_2025.FormFieldComponent">
      <t t-set="top_class" t-value="'my-3'" />
      <t t-set="label">Date of birth</t>
      <t t-set="input_id" t-value="'birthdate'" />
      <input
        id="birthdate"
        type="date"
        name="birthdate"
        class="form-control"
        t-att-value="wizard.birthdate"
        required="required"
      />
      <div class="mt-2 very-tiny-text text-mid-grey">
                Write&amp;Pray is for people under 25. This is the only extra question before you continue.
            </div>
    </t>
    <!-- Email -->
    <t t-call="theme_compassion_2025.FormFieldComponent">
      <t t-set="top_class" t-value="'my-3'" />
      <t t-set="label">E-mail</t>
      <t t-set="input_id" t-value="'email'" />
      <input
        id="email"
        type="email"
        name="email"
        class="form-control"
        placeholder="you@example.com"
        t-att-value="wizard.email"
        data-validate-type="email"
        required=""
      />
      <div class="mt-2 very-tiny-text text-mid-grey">For your documentation and&#160;<t
        t-out="wizard.child_id.preferred_name"
      />'s letters. Nothing else is needed right now.</div>
    </t>
    <!-- Which describes you -->
    <div class="my-3">
      <div class="mb-2 text-dark-blue body-small bold">Which describes you?</div>
      <label
        for="contribute-false"
        class="radio-card d-flex align-items-start p-3 mb-2 rounded rounded-lg bg-white border border-mid-grey"
      >
        <input
          type="radio"
          value="false"
          class="wap-contribute flex-shrink-0 mt-1"
          name="contribute"
          id="contribute-false"
          t-att-checked="wizard.wap_contribution_amount == 0"
        />
        <span class="ms-3 tiny-text">
                    I pray for and write to my godchild regularly. Someone else provides the financial support, but I
                    am the child's official sponsor.
                </span>
      </label>
      <label
        for="contribute-true"
        class="radio-card d-flex align-items-start p-3 rounded rounded-lg bg-white border border-mid-grey"
      >
        <input
          type="radio"
          value="true"
          class="wap-contribute flex-shrink-0 mt-1"
          name="contribute"
          id="contribute-true"
          t-att-checked="wizard.wap_contribution_amount != 0"
        />
        <span class="ms-3 tiny-text">
                    I pray for and write to my sponsored child regularly, and I can contribute monthly.
                </span>
      </label>
      <!-- Contribution amount -->
      <div id="wap-contribution-amount">
        <t
          t-set="amounts"
          t-value="wap_suggested_amounts or [
                    { 'name': 'low', 'amount': 5 },
                    { 'name': 'medium', 'amount': 10 },
                    { 'name': 'high', 'amount': 20 },
                ]"
        />
        <t t-call="theme_compassion_2025.FormFieldComponent">
          <t t-set="top_class" t-value="'mt-3 w-100'" />
          <t t-set="label_color" t-value="'dark-blue'" />
          <t t-set="label">Suggested amounts</t>
          <div class="mt-1 d-flex justify-content-between">
            <t t-foreach="amounts" t-as="amount">
              <t t-if="amount_index &gt; 0" t-set="margin" t-value="'ms-3'" />
              <div t-attf-class="contribution-suggested-amount flex-grow-1 #{margin}">
                <input
                  t-attf-id="suggested-#{amount['name']}"
                  type="radio"
                  t-attf-name="suggested_amount"
                  t-att-value="amount['amount']"
                  t-att-checked="wizard.wap_contribution_amount == amount['amount'] or (amount['name'] == 'low' and wizard.wap_contribution_amount == 0)"
                  class="suggested-amount"
                />
                <label class="mb-0 w-100" t-attf-for="suggested-#{amount['name']}">
                  <t t-out="currency_name" />
                  <t t-out="amount['amount']" />
                </label>
              </div>
            </t>
          </div>
        </t>
        <t t-call="theme_compassion_2025.FormFieldComponent">
          <t t-set="top_class" t-value="'mt-3 w-100'" />
          <t t-set="label_color" t-value="'dark-blue'" />
          <t t-set="label">Or enter your own amount</t>
          <!-- Hidden radio: keeps the existing suggested_amount/custom_amount
               submit contract (new.sponsorship.wizard._update_wizard_from_step)
               without a separate clickable "Custom amount" toggle - typing in
               the field below checks it directly (see _onCustomAmountInput). -->
          <input
            t-attf-id="suggested-custom"
            type="radio"
            t-attf-name="suggested_amount"
            value="custom"
            t-att-checked="wizard.wap_contribution_amount not in [amount['amount'] for amount in amounts] + [0]"
            class="suggested-amount d-none"
          />
          <input
            type="text"
            name="custom_amount"
            data-validate-type="number"
            t-att-placeholder="currency_name"
            class="form-control"
            t-att-value="'{:g}'.format(wizard.wap_contribution_amount) if wizard.wap_contribution_amount else ''"
          />
        </t>
      </div>
    </div>
    <!-- Privacy consent -->
    <input type="hidden" name="privacy_consent" value="" />
    <t t-call="theme_compassion_2025.FormFieldComponent">
      <t t-set="top_class" t-value="'my-3'" />
      <div class="d-flex align-items-center">
        <input
          type="checkbox"
          id="privacy_consent"
          name="privacy_consent"
          class="flex-shrink-0"
          t-att-checked="wizard.privacy_consent"
          required=""
        />
        <label class="ms-2 mb-0 tiny-text text-mid-grey" for="privacy_consent">
                    I accept the privacy policy as described in the
                    <a
          href="/legal"
          class="tiny-text bold text-core-blue"
        >privacy notice</a>.
                </label>
      </div>
    </t>
    <!-- Ends with the generic Continue button, rendered by
         my2_new_sponsorship_wizard_form_content - see the template-level
         comment above for why. -->
  </template>
  <!-- NEW SPONSORSHIP WIZARD WRITE & PRAY OPTIONS STEP -->
  <template
    id="my2_new_sponsorship_wizard_wap_options"
    name="New Sponsorship Write &amp; Pray Options Step"
  >
    <t t-if="wizard.birthdate">
      <input type="hidden" name="birthdate" t-att-value="wizard.birthdate" />
    </t>
    <t t-else="">
      <div class="alert alert-info mb-3 rounded-lg body-small">
                Write&amp;Pray sponsorships are available for people under the age of 25. Please provide your date of birth.
            </div>
      <t t-call="theme_compassion_2025.FormFieldComponent">
        <t t-set="top_class" t-value="'my-3'" />
        <t t-set="label">Date of birth</t>
        <t t-set="input_id" t-value="'birthdate'" />
        <input id="birthdate" type="date" name="birthdate" required="required" />
      </t>
    </t>
    <!-- Contribute -->
    <div class="my-3">
      <div class="ms-2 mt-2 d-flex">
        <input
          type="radio"
          value="false"
          class="wap-contribute flex-shrink-0"
          name="contribute"
          id="contribute-false"
          t-att-checked="wizard.wap_contribution_amount == 0"
        />
        <label class="mx-3 mb-0 body-small flex-grow-1" for="contribute-false">
                    I agree to pray for and write to my sponsored child regularly. I understand that another person is
                    providing
                    financial support for this sponsorship, but that I am the child's official sponsor.
                </label>
      </div>
      <div class="ms-2 mt-4 d-flex">
        <input
          type="radio"
          value="true"
          class="wap-contribute flex-shrink-0"
          name="contribute"
          id="contribute-true"
          t-att-checked="wizard.wap_contribution_amount != 0"
        />
        <label class="mx-3 mb-0 body-small flex-grow-1" for="contribute-true">
                    I commit to praying for and writing to my sponsored child regularly, and I can contribute monthly
                    in the amount of:
                </label>
      </div>
      <!-- Contribution amount -->
      <div id="wap-contribution-amount">
        <t
          t-set="amounts"
          t-value="wap_suggested_amounts or [
                    { 'name': 'low', 'amount': 5 },
                    { 'name': 'medium', 'amount': 10 },
                    { 'name': 'high', 'amount': 20 },
                ]"
        />
        <t t-call="theme_compassion_2025.FormFieldComponent">
          <t t-set="top_class" t-value="'mt-4 w-100'" />
          <t t-set="label">Suggested amounts</t>
          <div class="mt-1 d-flex justify-content-between">
            <t t-foreach="amounts" t-as="amount">
              <t t-if="amount_index &gt; 0" t-set="margin" t-value="'ms-3'" />
              <div t-attf-class="contribution-suggested-amount flex-grow-1 #{margin}">
                <input
                  t-attf-id="suggested-#{amount['name']}"
                  type="radio"
                  t-attf-name="suggested_amount"
                  t-att-value="amount['amount']"
                  t-att-checked="wizard.wap_contribution_amount == amount['amount'] or (amount['name'] == 'low' and wizard.wap_contribution_amount == 0)"
                  class="suggested-amount"
                />
                <label class="mb-0 w-100" t-attf-for="suggested-#{amount['name']}">
                  <t t-out="currency_name" />
                  <t t-out="amount['amount']" />
                </label>
              </div>
            </t>
          </div>
        </t>
        <t t-call="theme_compassion_2025.FormFieldComponent">
          <t t-set="top_class" t-value="'mt-3 w-100'" />
          <t t-set="label">Or enter your own amount</t>
          <div class="contribution-suggested-amount">
            <input
              t-attf-id="suggested-custom"
              type="radio"
              t-attf-name="suggested_amount"
              value="custom"
              t-att-checked="wizard.wap_contribution_amount not in [amount['amount'] for amount in amounts] + [0]"
              class="suggested-amount"
            />
            <label
              class="mb-0 w-100"
              t-attf-for="suggested-custom"
            >Custom amount</label>
          </div>
        </t>
        <t t-call="theme_compassion_2025.FormFieldComponent">
          <t t-set="top_class" t-value="'custom-amount-field mt-2 w-100'" />
          <input
            type="text"
            name="custom_amount"
            data-validate-type="number"
            t-att-placeholder="currency_name"
            class="form-control"
            t-att-value="'{:g}'.format(wizard.wap_contribution_amount) if wizard.wap_contribution_amount else ''"
            required=""
          />
        </t>
      </div>
    </div>
  </template>
  <!-- NEW SPONSORSHIP WIZARD COMMUNICATION DETAILS STEP -->
  <template
    id="my2_new_sponsorship_wizard_communication_details"
    name="New Sponsorship Wizard Communication Details Step"
  >
    <t t-call="theme_compassion_2025.FormFieldComponent">
      <t t-set="top_class" t-value="'my-3'" />
      <t t-set="label">Spoken languages</t>
      <div
      >For correspondence with my sponsored child, I also understand the following languages:</div>
      <t t-foreach="spoken_languages" t-as="language">
        <div class="ms-2 mt-2 d-flex align-items-center">
          <input
            type="checkbox"
            t-att-value="language.id"
            class="flex-shrink-0"
            t-attf-name="spoken_language_#{language.id}"
            t-att-id="'language_' + str(language.id)"
            t-att-checked="language in wizard.spoken_languages"
          />
          <label
            class="ms-2 mb-0 body-small"
            t-att-for="'language_' + str(language.id)"
          >
            <t t-out="language.name" />
          </label>
        </div>
      </t>
    </t>
    <!-- Lead source -->
    <t t-call="theme_compassion_2025.FormFieldComponent">
      <t t-set="top_class" t-value="'my-3'" />
      <t t-set="label">How did you hear about Compassion?</t>
      <t t-set="input_id" t-value="'lead_source'" />
      <t t-call="theme_compassion_2025.SelectComponent">
        <t t-set="name" t-value="'lead_source'" />
        <t t-set="id" t-value="'lead_source'" />
        <t t-set="required" t-value="True" />
        <option value="">Please select</option>
        <t t-foreach="lead_sources" t-as="lead_source">
          <option
            t-att-selected="lead_source.id == wizard.lead_source.id"
            t-att-value="lead_source.id"
          >
            <t t-out="lead_source.name" />
          </option>
        </t>
      </t>
    </t>
  </template>
  <!-- NEW SPONSORSHIP WIZARD FORM CONTENT -->
  <template
    id="my2_new_sponsorship_wizard_form_content"
    name="New Sponsorship Wizard Dynamic Content"
  >
    <input type="hidden" name="wizard_id" t-att-value="wizard.id" />
    <!-- PROGRESS BAR -->
    <!-- Rendered only once there is more than one step to progress through:
         a single-step flow (fast-checkout, or a single-step logged-in flow)
         has nothing to show progress against, and a lone marker just reads
         as a stray bullseye sitting above the title. -->
    <div t-if="wizard.n_steps > 1" class="mt-3 mb-2 px-2 px-lg-0 pt-2 w-100">
      <t t-call="my_compassion.NewSponsorshipStepProgressBar">
        <t t-set="n_steps" t-value="wizard.n_steps" />
        <t t-set="current_step" t-value="wizard.current_step_idx" />
      </t>
    </div>
    <!-- TITLE -->
    <div class="mx-5 mt-4">
      <t t-call="theme_compassion_2025.TitleComponent">
        <!-- The fast-checkout page's step title ("Your email") is generic
             DB data shared with nothing else; the page itself is centered
             on one child, so it gets a title naming them instead. -->
        <t
          t-if="step_offers_payment_modes"
          t-set="title_content"
          t-value="'Sponsor ' + (wizard.child_id.preferred_name or '')"
        />
        <t t-else="" t-set="title_content" t-value="wizard.current_step.title" />
        <t t-set="color" t-value="'core-blue'" />
        <t t-set="show_underline" t-value="True" />
        <t t-set="underline_color" t-value="'low-yellow'" />
        <t
          t-if="wizard.sponsorship_type == 'write_and_pray'"
          t-set="description"
        >Let's set up your Write&amp;Pray and introduce you to&#160;<t
          t-out="wizard.child_id.preferred_name"
        />.</t>
        <t
          t-else=""
          t-set="description"
        >Let's set up your sponsorship and introduce you to&#160;<t
          t-out="wizard.child_id.preferred_name"
        />.</t>
      </t>
    </div>
    <div class="p-1 d-flex flex-column">
      <t t-out="inner_step_html" />
      <!-- BUTTONS -->
      <!-- A step that offers payment modes submits itself with them: one
           button per mode, pressing one both picks the mode and ends the
           flow, so the generic button would be a second, ambiguous submit.
           They are rendered here rather than inside the step template so
           that "which modes are offered" and "is the generic button still
           needed" stay one decision in one place. The pressed mode is what
           my2_new_sponsorship_wizard.js posts, through the same step call as
           any other button. -->
      <t t-if="payment_mode_buttons">
        <!-- eBill and Postfinance Direct Debit each keep their own brand
             color instead of one from the theme palette; every other mode
             stays core-blue. Postfinance is matched by xmlid rather than
             payment_method_id.code: CH's LSV and Postfinance Direct Debit
             modes can share the same method code ("sepa.ch.dd") and are
             only told apart by xmlid (see CH_PENDING_PAYMENT_MODES in
             my_compassion_switzerland/models/contracts.py, same reason). -->
        <t
          t-set="postfinance_dd_xmlids"
          t-value="[
                        'sponsorship_switzerland.payment_mode_postfinance_dd',
                        'sponsorship_switzerland.payment_mode_postfinance_dd_25th',
                        'sponsorship_switzerland.payment_mode_postfinance_dd_multi_months',
                    ]"
        />
        <div class="payment-mode-buttons my-3 d-flex flex-column">
          <t t-foreach="payment_mode_buttons" t-as="payment_mode">
            <t t-if="payment_mode_index &gt; 0" t-set="spacing" t-value="'mt-2'" />
            <div
              t-attf-class="btn-next d-flex #{spacing}"
              data-action="next"
              t-att-data-sponsorship-type="wizard.sponsorship_type"
              t-att-data-payment-mode="payment_mode.id"
              t-att-data-payment-code="payment_mode.payment_method_id.code"
            >
              <t
                t-if="payment_mode.payment_method_id.code == 'ebill'"
                t-set="brand_class"
                t-value="'btn-ebill'"
              />
              <t
                t-elif="payment_mode.get_external_id().get(payment_mode.id) in postfinance_dd_xmlids"
                t-set="brand_class"
                t-value="'btn-postfinance-dd'"
              />
              <t t-else="" t-set="brand_class" t-value="False" />
              <t t-call="theme_compassion_2025.ThemedButtonComponent">
                <t t-set="variant" t-value="'compact'" />
                <t t-set="variation" t-value="'filled'" />
                <!-- #FFCC00 is too bright for white text to read on. -->
                <t
                  t-set="text_color"
                  t-value="'dark-blue' if brand_class == 'btn-postfinance-dd' else 'pure-white'"
                />
                <!-- A mode with a provider charges the first month right
                     after this click; a bank-collected one never does. -->
                <t
                  t-if="payment_mode.payment_provider_id"
                  t-set="label"
                >Pay with&#160;<t t-out="payment_mode.name" /></t>
                <t t-else="" t-set="label" t-value="payment_mode.name" />
                <t t-set="add_class" t-value="brand_class" />
                <t t-set="color" t-value="'core-blue'" />
              </t>
            </div>
          </t>
        </div>
      </t>
      <!-- The generic button stands in only for the steps whose submit it
           actually is. A step submitted by its payment modes that has none
           to show gets no submit at all: falling back here would finish a
           standard sponsorship with no payment mode, collected by nothing,
           behind a thank-you page saying it started. -->
      <div
        t-elif="not step_offers_payment_modes"
        class="btn-next my-3 d-flex justify-content-center"
        data-action="next"
        t-att-data-sponsorship-type="wizard.sponsorship_type"
      >
        <t t-call="theme_compassion_2025.ThemedButtonComponent">
          <t t-set="variant" t-value="'compact'" />
          <t t-set="variation" t-value="'filled'" />
          <t
            t-if="wizard.current_step_idx &lt; (wizard.n_steps-1) or wizard.sponsorship_type == 'write_and_pray'"
            t-set="label"
          >Continue</t>
          <t t-else="" t-set="label">Finish</t>
          <t t-set="color" t-value="'core-blue'" />
          <t t-set="text_color" t-value="'pure-white'" />
          <!-- ID used by eBill extension in my_compassion_switzerland -->
          <t t-set="id" t-value="'finish_button'" />
        </t>
      </div>
      <div
        t-if="wizard.current_step_idx &gt; 0"
        class="btn-previous mt-2 mb-3 d-flex justify-content-center"
        data-action="previous"
        t-att-data-sponsorship-type="wizard.sponsorship_type"
      >
        <button class="back-button text-low-black body-small bold">Back</button>
      </div>
    </div>
  </template>
  <!-- NEW SPONSORSHIP WIZARD PAGE -->
  <template id="my2_new_sponsorship_wizard_page" name="New Sponsorship Wizard Page">
    <t t-call="website.layout">
      <div class="new-sponsorship-container">
        <form
          class="new-sponsorship-wizard-form"
          action="/my2/new-sponsorship/submit"
          method="POST"
        >
          <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()" />
          <div class="new-sponsorship-wizard-form-content">
            <!-- INITIAL FORM CONTENT -->
            <t t-out="form_content_html" />
          </div>
        </form>
      </div>
    </t>
  </template>
  <!-- NEW SPONSORSHIP THANK YOU PAGE -->
  <template
    id="my2_new_sponsorship_thank_you_page"
    name="New Sponsorship Thank You Page"
  >
    <t t-call="website.layout">
      <div class="container">
        <div class="single-column d-flex flex-column align-items-center">
          <!-- PROGRESS BAR -->
          <!-- Only rendered past one step, same as the wizard form itself
               (my2_new_sponsorship_wizard_form_content): a single-step flow
               has nothing to show progress against, and a lone marker just
               reads as a stray bullseye. -->
          <div t-if="n_steps &gt; 1" class="mt-3 mb-2 px-2 px-lg-0 pt-2 w-100">
            <t t-call="my_compassion.NewSponsorshipStepProgressBar">
              <t t-set="n_steps" t-value="n_steps" />
              <t t-set="current_step" t-value="n_steps + 1" />
            </t>
          </div>
          <!-- TITLE -->
          <div class="mx-4">
            <t t-call="theme_compassion_2025.TitleComponent">
              <t t-set="title_content">Thank you for sponsoring&#160;<t
                t-out="sponsorship.child_id.preferred_name"
              /></t>
              <t t-set="color" t-value="'core-blue'" />
              <t t-set="show_underline" t-value="True" />
              <t t-set="underline_color" t-value="'low-yellow'" />
              <t
                t-if="sponsorship.type == 'SWP'"
                t-set="description"
              >You’ll receive an email with everything you need to start the Write&amp;Pray soon.</t>
              <t t-else="" t-set="description">
                                You’ll receive an email with everything you need to start the sponsorship soon.
                            </t>
            </t>
          </div>
          <!-- The signup still waits for its sponsor's name: ask for it here
               instead of inviting them to create an account they cannot be
               greeted in yet. The controller only hands over a token while
               that is the case, so this branch closes by itself. -->
          <t t-if="details_token">
            <t t-call="my_compassion.my2_new_sponsorship_details_form" />
          </t>
          <t t-elif="details_emailed">
            <t t-call="my_compassion.my2_new_sponsorship_details_emailed" />
          </t>
          <t t-elif="request.env.user._is_public()">
            <!-- "All set" summary: the portal invitation (sign-in link) was
                 already emailed by now - see recurring.contract
                 ._my2_send_portal_invitation, triggered off the placeholder
                 name being replaced (res.partner
                 ._my2_on_placeholder_name_replaced), which is exactly what
                 completing the "Who shall we thank?" form does. Nothing here
                 sends it; this just tells the sponsor it happened. -->
            <t
              t-set="sponsorship_plus_line"
              t-value="sponsorship.contract_line_ids.filtered(lambda l: l.product_id.default_code == 'fund_gen')"
            />
            <div class="mx-4">
              <t t-call="theme_compassion_2025.TitleComponent">
                <t t-set="title_content">All set,&#160;<t
                  t-out="sponsorship.partner_id.firstname"
                /></t>
                <t t-set="color" t-value="'dark-blue'" />
                <t t-set="show_underline" t-value="False" />
              </t>
            </div>
            <div class="mb-3 text-center body-small text-low-black">
              We'll send a sign-in link to&#160;<t
              t-out="sponsorship.partner_id.email"
            />&#160;to set up a password, and you'll have access to your account.
            </div>
            <div class="w-100 p-4 mb-3 rounded-lg bg-light-blue">
              <!-- Write&Pray never collects a payment, so it has nothing to
                   show under "First payment" - "Commitment: Confirmed" names
                   what actually happened instead, and "Role" replaces the
                   implied-sponsor framing the standard rows use. -->
              <t t-if="sponsorship.type == 'SWP'">
                <div class="d-flex justify-content-between mb-2">
                  <span class="body-small text-mid-grey">Child</span>
                  <span class="body-small bold text-dark-blue"><t
                    t-out="sponsorship.child_id.preferred_name"
                  />, <t
                    t-out="sponsorship.child_id.field_office_id.country_id.name"
                  /></span>
                </div>
                <div class="d-flex justify-content-between mb-2">
                  <span class="body-small text-mid-grey">Role</span>
                  <span class="body-small bold text-dark-blue">
                    <t
                      t-if="sponsorship.total_amount"
                    >Sponsor, writes and contributes</t>
                    <t t-else="">Godparent, writes only</t>
                  </span>
                </div>
                <div class="d-flex justify-content-between mb-2">
                  <span class="body-small text-mid-grey">Monthly gift</span>
                  <span class="body-small bold text-dark-blue">
                    <t t-if="sponsorship.total_amount">
                      <t t-out="sponsorship.currency_id.name" />
                      &#160;
                      <t t-out="'%.2f' % sponsorship.total_amount" />
                    </t>
                    <t t-else="">No payment</t>
                  </span>
                </div>
                <div class="d-flex justify-content-between mb-2">
                  <span class="body-small text-mid-grey">Commitment</span>
                  <span class="body-small bold text-mid-green">Confirmed</span>
                </div>
                <div class="d-flex justify-content-between">
                  <span class="body-small text-mid-grey">Contact details</span>
                  <span class="body-small bold text-mid-green">Complete</span>
                </div>
              </t>
              <t t-else="">
                <div class="d-flex justify-content-between mb-2">
                  <span class="body-small text-mid-grey">Sponsored child</span>
                  <span class="body-small bold text-dark-blue"><t
                    t-out="sponsorship.child_id.preferred_name"
                  />, <t
                    t-out="sponsorship.child_id.field_office_id.country_id.name"
                  /></span>
                </div>
                <div class="d-flex justify-content-between mb-2">
                  <span class="body-small text-mid-grey">Monthly gift</span>
                  <span class="body-small bold text-dark-blue">
                    <t t-out="sponsorship.currency_id.name" />
                    &#160;
                    <t t-out="'%.2f' % sponsorship.total_amount" />
                  </span>
                </div>
                <div
                  t-if="sponsorship_plus_line"
                  class="d-flex justify-content-between mb-2"
                >
                  <span class="tiny-text text-mid-grey">Sponsorship Plus</span>
                  <span class="tiny-text bold text-core-blue">Included&#160;·&#160;<t
                    t-out="sponsorship.currency_id.name"
                  />&#160;<t t-out="'%.2f' % sponsorship_plus_line.amount" /></span>
                </div>
                <div class="d-flex justify-content-between mb-2">
                  <span class="body-small text-mid-grey">First payment</span>
                  <span
                    t-attf-class="body-small bold text-#{'mid-green' if sponsorship.state == 'active' else 'mid-orange'}"
                  >
                    <t
                      t-out="'Received' if sponsorship.state == 'active' else 'Pending'"
                    />
                  </span>
                </div>
                <div class="d-flex justify-content-between">
                  <span class="body-small text-mid-grey">Contact details</span>
                  <span class="body-small bold text-mid-green">Complete</span>
                </div>
              </t>
            </div>
            <!-- Skipped for Write&amp;Pray: the details box right above
                 already names the child, and the wap-preferences-card /
                 volunteering card below it are anchored independently of
                 this one, so nothing else depends on it being present. -->
            <div t-if="sponsorship.type != 'SWP'" class="all-set-child-card w-100 mb-3">
              <t t-call="my_compassion.DonationItemComponent">
                <t t-set="name" t-value="sponsorship.child_id.preferred_name" />
                <t
                  t-set="subtext"
                  t-value="sponsorship.child_id.field_office_id.country_id.name"
                />
                <t
                  t-set="image"
                  t-valuef="/web/image/compassion.child/#{sponsorship.child_id.id}/portrait"
                />
                <t
                  t-set="image_fallback_pictogram"
                  t-value="'pictogram-child-sponsorship'"
                />
                <t t-set="price" t-value="sponsorship.total_amount" />
                <t t-set="currency_name" t-value="sponsorship.currency_id.name" />
                <t t-set="monthly_noabbr" t-value="True" />
                <t t-set="bg_color" t-value="'light-blue'" />
                <t t-set="accent_color" t-value="'low-blue'" />
                <t t-set="title_color" t-value="'low-blue'" />
                <t t-set="price_color" t-value="'low-blue'" />
              </t>
            </div>
            <!-- Anchored on by my_compassion_switzerland's volunteering
                 opt-in (new_sponsorship_wizard_volunteering) via
                 wap-preferences-card, so it lands between this card and the
                 dashboard button below regardless of whether this card
                 itself renders (t-if false outside SWP or repeat W&amp;P). -->
            <t
              t-if="sponsorship.type == 'SWP'"
              t-set="other_wap_count"
              t-value="request.env['recurring.contract'].sudo().search_count([
                    ('partner_id', '=', sponsorship.partner_id.id),
                    ('type', '=', 'SWP'),
                    ('id', '!=', sponsorship.id),
                ])"
            />
            <div
              t-if="sponsorship.type == 'SWP' and not other_wap_count"
              class="wap-preferences-card w-100 p-3 mb-3 rounded rounded-lg bg-high-eggshell border border-low-yellow"
            >
              <div
                class="body-small bold text-dark-blue mb-2"
              >Write your first letter?</div>
              <div class="tiny-text text-mid-grey mb-3">
                            Now that you're all set, why not send&#160;<t
                t-out="sponsorship.child_id.preferred_name"
              />&#160;a first hello? A letter means the world to them.
                        </div>
              <div class="mb-2 text-center">
                <a t-att-href="first_letter_url">
                  <t t-call="theme_compassion_2025.ThemedButtonComponent">
                    <t t-set="variant" t-value="'compact'" />
                    <t t-set="variation" t-value="'filled'" />
                    <t t-set="label">Write my first letter</t>
                    <t t-set="color" t-value="'core-blue'" />
                    <t t-set="text_color" t-value="'pure-white'" />
                  </t>
                </a>
              </div>
            </div>
            <div class="mb-3">
              <a href="/web/login?redirect=/my/home">
                <t t-call="theme_compassion_2025.ThemedButtonComponent">
                  <t t-set="variant" t-value="'compact'" />
                  <t t-set="variation" t-value="'filled'" />
                  <t t-set="label">Go to my dashboard</t>
                  <t t-set="color" t-value="'core-blue'" />
                  <t t-set="text_color" t-value="'pure-white'" />
                </t>
              </a>
            </div>
          </t>
          <t t-else="">
            <!-- REVIEW PREFERENCES -->
            <div class="mb-4 text-center body-small">
                            Compassion manages your communication with
                            <t t-out="sponsorship.child_id.preferred_name" />
                            .
                            You can adjust your preferences at any time in your user settings for a tailored experience.
                            <span
              class="body-small bold"
            >Would you like to review them now?</span>
                        </div>
            <div class="mb-3">
              <a href="/my2/user_settings?current_tab=communication-settings">
                <t t-call="theme_compassion_2025.ThemedButtonComponent">
                  <t t-set="variant" t-value="'compact'" />
                  <t t-set="variation" t-value="'filled'" />
                  <t t-set="label">Review my preferences now</t>
                  <t t-set="color" t-value="'core-blue'" />
                  <t t-set="text_color" t-value="'pure-white'" />
                </t>
              </a>
            </div>
            <div class="mb-4">
              <t
                t-if="sponsorship.type == 'SWP'"
                t-set="route"
                t-valuef="/my2/write-and-pray"
              />
              <t t-else="" t-set="route" t-valuef="/my2/sponsorships" />
              <button
                class="back-button text-low-black body-small bold"
                t-attf-onclick="location.href='#{route}'"
              >
                                I'll do this later
                            </button>
            </div>
          </t>
        </div>
      </div>
    </t>
  </template>
</odoo>
