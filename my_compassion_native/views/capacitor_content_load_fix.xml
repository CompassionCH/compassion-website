<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- Fixes FOUC (Flash of Unstyled Content) in Capacitor-based native apps -->
    <template id="capacitor_fouc_fix" inherit_id="website.layout" name="Capacitor FOUC Fix">
        <xpath expr="//head" position="inside">
            <script type="text/javascript">
                if (window.Capacitor &amp;&amp; window.Capacitor.getPlatform() !== 'web') {
                    document.documentElement.style.opacity = '0';
                    document.documentElement.style.backgroundColor = '#ffffff';

                    window.addEventListener('load', function() {
                        document.documentElement.style.transition = 'opacity 0.25s ease-in-out';
                        document.documentElement.style.opacity = '1';
                    });

                    setTimeout(function() {
                        if (document.documentElement.style.opacity === '0') {
                            document.documentElement.style.transition = 'opacity 0.25s ease-in-out';
                            document.documentElement.style.opacity = '1';
                        }
                    }, 1000);
                }
            </script>
        </xpath>
    </template>
</odoo>
